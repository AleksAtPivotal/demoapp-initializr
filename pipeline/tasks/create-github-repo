#!/bin/sh

set -eu

root=$PWD


tempfolder=$(mktemp -d)
cp demoapp-initializr/pipeline/terraform/create_github_repo.tf $tempfolder/terraform.tf
pushd $tempfolder

terraform init 
terraform apply \
    --var reponame=$REPONAME \
    --var github_token=$GITHUBTOKEN 
